fun main() {
    println("Hello World!")
}

// 2 EUR + 4 USD = 8 USD
// to big amount (more than double)
// conversion with fractional part round to 4 decimal places
// Wallet(dollar(30), franc(20)) = Wallet(franc(20), dollar(30))
// Wallet(dollar(30)) + franc(20) = Wallet(dollar(30), franc(20))
// Wallet(dollar(30), franc(20)) + franc(20) = Wallet(dollar(30), franc(40))
